# Backend Configuration
BACKEND_URL=http://localhost:3001

# Database Configuration
# Frontend uses backend API, backend connects to PostgreSQL
# PostgreSQL runs in Docker container (see docker-compose.yaml)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/poam_tracker"

# JWT Configuration
JWT_SECRET=your-jwt-secret-key-here

# Next.js Configuration
NEXT_PUBLIC_APP_URL=http://localhost:3000
NODE_ENV=development

# Frontend URL for CORS (used by backend)
FRONTEND_URL=http://localhost:3000